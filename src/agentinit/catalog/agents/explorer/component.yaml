description: Read-only codebase research. Use proactively to locate code, trace call paths or answer "where/how does X work" without filling the main context.
tools: [Read, Grep, Glob, Bash]
model: haiku
